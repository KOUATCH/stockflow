/**
 * Compatibility placeholder.
 *
 * Server monitoring is bootstrapped from app/layout.tsx so this shared
 * component never imports server-only or Prisma-backed modules.
 */

export function SystemMonitoring() {
  return null;
}

export default SystemMonitoring;
